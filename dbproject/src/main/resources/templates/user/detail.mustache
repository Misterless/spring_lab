{{> /layout/header}}

<!-- 컨테이너 시작 -->
<div class="container mt-3">

    <!-- 상세정보보기 시작 -->
    
        <div class="mb-3 mt-3">
            {{user.username}}
        </div>
        <div class="mb-3">
            {{user.password}}
        </div>
        <div class="mb-3 ">
            {{user.email}}
        </div>
            </label>
        </div>
        <div>
        <a href="/user/{{user.id}}/updateForm" class="btn btn-info">수정페이지로</button>
    </div>
    
    <!-- 상세정보보기 -->

</div>
<!-- 컨테이너 끝 -->

{{> /layout/footer}}